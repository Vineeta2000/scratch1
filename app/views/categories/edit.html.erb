<h1>Edit Categories </h1>
<%= form_with model: @category, url:   category_path, method: :put do |form| %>
<div><strong>
	<%= form.label :name %> ~<br> </strong>
	<%= form.text_field :name %>
</div>

<div>
	<%= form.submit "Update" %>
</div>
<% end %>
<%= link_to "Back" , root_path %>
